# 🏗️ Arquitectura WebSocket Migration - Diagramas

## 1. Comparación: Webhooks vs WebSocket

### ANTES: Webhooks HTTP (6-12 segundos) 🐢

```
┌─────────────┐
│   Usuario   │ habla: "hola"
│  (Teléfono) │
└──────┬──────┘
       │ audio stream
       ▼
┌──────────────────┐
│  Telnyx API      │
│  Call Control    │ espera 1000-2000ms silencio
└──────┬───────────┘
       │ HTTP POST: transcription webhook
       ▼
┌──────────────────┐
│  Deepgram        │
│  (vía Telnyx)    │ latencia inherente
└──────┬───────────┘
       │
       │ genera: "hola" (final transcript)
       │ espera silencio (utterance_end)
       │
       ▼
┌──────────────────────┐
│  Tu Servidor         │
│  /webhook/telnyx     │ recibe después de 6-12 segundos
│  (HTTP POST)         │
└──────┬───────────────┘
       │ procesa transcript
       ▼
┌──────────────────┐
│  Claude (IA)     │ genera respuesta
│  (1-1.5 segundos)│
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  ElevenLabs TTS  │ sintetiza audio
│  (0.5 segundos)  │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  AWS S3          │ almacena audio
│  (0.3 segundos)  │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  Telnyx Playback │ reproduce audio
│  (0.2 segundos)  │
└──────┬───────────┘
       │
       ▼
┌──────────────┐
│  Usuario     │ escucha respuesta después de 8-14 segundos total
│  (Teléfono)  │ (LENTO ❌)
└──────────────┘
```

**Bottleneck Principal**: Espera de silencio en Deepgram (1000-2000ms)
**Latencia Total**: 8-14 segundos

---

### AHORA: WebSocket Media Streams (1-2 segundos) ⚡

```
┌─────────────┐
│   Usuario   │ habla: "hola"
│  (Teléfono) │
└──────┬──────┘
       │ audio stream (mulaw 8kHz)
       │
       ▼
┌────────────────────────────┐
│  Telnyx Media Stream WS     │ <--┐ bidirecional
│  (WebSocket WSS)            │   │ en tiempo real
└──────┬──────────────────────┘   │ sin latencia
       │                          │
       │ (< 500ms)                │
       │                          │
       ▼                          │
   ╔═══════════════════╗          │
   ║  Tu Servidor      ║◄────────┘
   ║  /stream/media    ║ <-- WebSocket handler
   ║  (media_stream.rs)║
   ╚═════┬─────────────╝
         │
         │ envía audio al
         │ tiempo que recibe
         │
         ▼
┌──────────────────────────┐
│  Deepgram WebSocket      │ <--┐ bidirecional
│  (conexión directa)      │   │ streaming
│  (nova-2 model)          │   │ < 500ms
└──────┬───────────────────┘   │
       │                       │
       │ (endpointing: 200ms)  │
       │ NO espera silencio    │
       │                       │
       ▼                       │
   ╔════════════════════════╗  │
   ║  Tu Servidor           ║◄─┘
   ║  recibe INMEDIATAMENTE │
   ║  transcript intermedio │
   ║  (≥3 palabras)         │
   ╚════════┬───────────────╝
           │ procesa mientras usuario
           │ aún está hablando
           ▼
┌──────────────────┐
│  Claude (IA)     │ genera respuesta en paralelo
│  (1-1.5 seg)     │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  ElevenLabs TTS  │ prepara audio
│  (0.5 seg)       │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  AWS S3          │ almacena
│  (0.3 seg)       │
└──────┬───────────┘
       │
       ▼
┌──────────────────┐
│  Telnyx Playback │ reproduce
│  (0.2 seg)       │
└──────┬───────────┘
       │
       ▼
┌──────────────┐
│  Usuario     │ escucha respuesta después de 1-2 segundos
│  (Teléfono)  │ (¡CASI EN TIEMPO REAL! ⚡)
└──────────────┘
```

**Sin Bottleneck**: Procesamiento paralelo en tiempo real
**Latencia Total**: 1-2 segundos

---

## 2. Flujo Detallado: WebSocket Media Streams

```
INICIAL (t=0ms)
──────────────────────────────────────────────────────────────

Usuario marca número → Telnyx InitiateCall
                         ↓
                    Busca WEBHOOK_BASE_URL
                    Convierte:
                    https://domain.com
                    ↓
                    wss://domain.com/stream/media
                    ↓
                    Establece WebSocket upgrade

t=100ms - Conexión WSS establecida
──────────────────────────────────────────────────────────────

Telnyx envía evento "start":
{
  "event": "start",
  "start": {
    "call_control_id": "abc123",
    "direction": "inbound",
    ...
  }
}
                    ↓
            Tu servidor recibe
            Extrae call_control_id: "abc123"
                    ↓
            Conecta a Deepgram WSS
            (Bearer token con API key)
                    ↓
            Spawned Task 1: Audio forwarder
            Spawned Task 2: Transcript processor
            Spawned Task 3: Greeting playback (delay 500ms)

t=200ms - Audio Forwarding Comienza
──────────────────────────────────────────────────────────────

Telnyx envía eventos "media":
{
  "event": "media",
  "media": {
    "payload": "AQIDBA==" (base64 mulaw)
  }
}
                    ↓
            Task 1 decodifica base64
                    ↓
            Envía bytes mulaw a Deepgram WSS
                    ↓
            Deepgram procesa en tiempo real

t=300ms - Greeting Playback
──────────────────────────────────────────────────────────────

Task 3 ejecuta:
- Genera: "Hola, ¿cómo estás?"
- ElevenLabs TTS
- Sube a S3
- Telnyx.playAudio()

Usuario escucha greeting mientras sigue hablando

t=500ms - Primeras Palabras Transcritas
──────────────────────────────────────────────────────────────

Usuario dice: "quiero hablar con ventas"

Deepgram detecta palabras (VAD: 200ms endpointing)
Envía transcript intermedio:
{
  "channel": {
    "alternatives": [{
      "transcript": "quiero",
      "confidence": 0.98
    }]
  },
  "is_final": false
}
                    ↓
            Task 2 recibe (word_count: 1)
            No procesa aún (< 3 palabras)

t=700ms - 3+ Palabras Detectadas
──────────────────────────────────────────────────────────────

Usuario continúa: "quiero hablar con ventas"

Deepgram envía:
{
  "transcript": "quiero hablar con",
  "is_final": false
}
                    ↓
            Task 2 recibe (word_count: 3) ✓
            Texto: "quiero hablar con" (length: 18) ✓
            Pasa filtros → PROCESA
                    ↓
            Envía a Claude:
            "El usuario dijo: quiero hablar con"
                    ↓
            Claude IA procesa en paralelo
            (mientras usuario sigue hablando)
            Claude responde: "Conectando con ventas..."

t=1200ms - Respuesta de Claude Lista
──────────────────────────────────────────────────────────────

Claude completó respuesta
ElevenLabs TTS sintetiza
Audio almacenado en S3
Playback iniciado

Usuario escucha respuesta

t=1500ms - Respuesta Entregada
──────────────────────────────────────────────────────────────

Total tiempo desde que usuario habló:
t=500ms (transcripción) - t=1500ms (respuesta) = 1000ms
+ tiempo de audio processing: 200ms
TOTAL: ~1.2 segundos ⚡

Usuario percibe respuesta "casi en tiempo real"

Final (t=∞ms) - Usuario cuelga
──────────────────────────────────────────────────────────────

Usuario termina llamada
Telnyx envía evento "stop"
                    ↓
            Tu servidor:
            - Cierra WebSocket con Deepgram
            - Limpia session
            - Cancela tasks pendientes
                    ↓
            Conexión completa
```

---

## 3. Diagrama de Componentes

```
┌─────────────────────────────────────────────────────────────┐
│                        TELNYX PLATFORM                       │
├─────────────────────────────────────────────────────────────┤
│                                                               │
│  ┌──────────────────┐                ┌────────────────────┐ │
│  │ Call Control API │                │ Media Stream (WSS) │ │
│  └────────┬─────────┘                └─────────┬──────────┘ │
│           │                                     │            │
│  initiate_call()                   WebSocket upgrade        │
│  stream_url + stream_track         /stream/media            │
│           │                                     │            │
└───────────┼─────────────────────────────────────┼────────────┘
            │                                     │
            ▼                                     ▼
┌────────────────────────────────────────────────────────────┐
│              TU SERVIDOR RUST (Axum)                        │
├────────────────────────────────────────────────────────────┤
│                                                              │
│  ┌──────────────┐              ┌──────────────────────┐   │
│  │ Call Handler │              │ WebSocket Handler    │   │
│  │ (call.rs)    │              │ (media_stream.rs)    │   │
│  └──────┬───────┘              └──────┬───────────────┘   │
│         │                             │                   │
│    check env:                    handle_socket()          │
│    USE_MEDIA_STREAMS             │                       │
│         │                         ├─ Task 1: Audio fwd  │
│         ├─ true                   ├─ Task 2: Transcript │
│         │   └─initiate_call_with  ├─ Task 3: Greeting  │
│         │     stream()            │                      │
│         │                         ▼                      │
│         └─ false                ┌──────────────────────┐ │
│           └─initiate_call()     │ Session Manager      │ │
│                                 │ (session.rs)         │ │
│                                 └──────────────────────┘ │
│                                                            │
│  ┌─────────────────────────────────────────────────────┐ │
│  │ Services                                            │ │
│  ├─────────────────────────────────────────────────────┤ │
│  │ ┌──────────────┐  ┌──────────────┐  ┌───────────┐ │ │
│  │ │DeepgramWS    │  │Claude        │  │ElevenLabs │ │ │
│  │ │(deepgram_ws  │  │(claude.rs)   │  │(tts.rs)   │ │ │
│  │ │.rs)          │  │              │  │           │ │ │
│  │ └──────────────┘  └──────────────┘  └───────────┘ │ │
│  │ ┌──────────────┐  ┌──────────────┐              │ │
│  │ │S3            │  │Telnyx        │              │ │
│  │ │(s3.rs)       │  │(telnyx.rs)   │              │ │
│  │ └──────────────┘  └──────────────┘              │ │
│  └─────────────────────────────────────────────────────┘ │
│                                                            │
└────────────────────────────────────────────────────────────┘
            │                              │
            │ WSS connection              │ REST API calls
            ▼                              ▼
┌──────────────────────────────────┐  ┌──────────────────┐
│     DEEPGRAM API                 │  │ CLAUDE API       │
│     WebSocket Streaming          │  │ (Anthropic)      │
│     (nova-2 model)               │  │                  │
└──────────────────────────────────┘  └──────────────────┘
                                          │
                                          ▼
            ┌───────────────────────────────────────────┐
            │ EXTERNAL SERVICES                         │
            ├───────────────────────────────────────────┤
            │ ┌──────────────┐  ┌───────────────────┐  │
            │ │ElevenLabs    │  │AWS S3             │  │
            │ │TTS API       │  │Audio Storage      │  │
            │ └──────────────┘  └───────────────────┘  │
            └───────────────────────────────────────────┘
```

---

## 4. Flujo de Procesamiento de Transcripts

```
Audio entra (mulaw 8kHz, mono)
│
├─────────────────────────────────────────┐
│ Deepgram WebSocket Task (Task 1)        │
│ - Decodificar base64 de Telnyx          │
│ - Enviar bytes a Deepgram WSS           │
│ - Mantener conexión abierta             │
└─────────────────────────────────────────┘
│
├─────────────────────────────────────────┐
│ Deepgram Streaming Response             │
│ (múltiples mensajes JSON)               │
│ - Interim results (is_final: false)     │
│ - Final results (is_final: true)        │
└─────────────────────────────────────────┘
│
├─────────────────────────────────────────┐
│ Transcript Receiver Task (Task 2)       │
│                                         │
│ for each message:                       │
│   - Parse JSON                          │
│   - Extract transcript text             │
│   - Extract confidence score            │
└─────────────────────────────────────────┘
│
├─────────────────────────────────────────┐
│ Filtering Logic                         │
│                                         │
│ word_count >= 3 OR is_final?            │
│   ├─ YES → PROCESS ✓                   │
│   └─ NO → SKIP (wait for more)         │
│                                         │
│ confidence >= 0.6?                      │
│   ├─ YES → CONTINUE                    │
│   └─ NO → LOG WARNING                  │
│                                         │
│ text.len() >= 10 chars?                 │
│   ├─ YES → CONTINUE                    │
│   └─ NO → SKIP (too short)             │
└─────────────────────────────────────────┘
│
├─────────────────────────────────────────┐
│ PROCESS TRANSCRIPT:                     │
│ - Log: "⚡ Processing intermediate"    │
│ - Send to Claude AI                    │
│ - Generate response                    │
│ - TTS synthesis                        │
│ - Store in S3                          │
│ - Queue for playback                   │
│ - Send to Telnyx                       │
└─────────────────────────────────────────┘
│
└─────────────────────────────────────────────► User hears response in ~1-2 seconds
```

---

## 5. Diagrama de Estados: WebSocket Connection

```
START
  │
  ▼
┌──────────────────────────────┐
│ HTTP GET /stream/media       │
│ (Connection: Upgrade header) │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ HTTP 101 Switching Protocols │
│ Upgrade to WebSocket         │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ WAITING_FOR_START            │
│ (receive Telnyx start event) │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ START_RECEIVED               │
│ Extract call_control_id      │
│ Create session               │
│ Connect to Deepgram          │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ DEEPGRAM_CONNECTED           │
│ Spawn tasks:                 │
│ - Audio forwarding           │
│ - Transcript processing      │
│ - Greeting playback          │
└──────────┬───────────────────┘
           │
           ▼
┌──────────────────────────────┐
│ ACTIVE                       │
│ Process audio & transcripts  │
│ (main processing loop)       │
└──────────┬───────────────────┘
           │
    ┌──────┴──────┐
    │             │
    ▼             ▼
ERROR         STOP_RECEIVED
(reconnect)   (user hangs up)
    │             │
    └─────┬───────┘
          │
          ▼
┌──────────────────────────────┐
│ CLEANUP                      │
│ - Close Deepgram WS          │
│ - Cancel pending tasks       │
│ - Close session              │
│ - Log statistics             │
└──────────┬───────────────────┘
           │
           ▼
        CLOSED
```

---

## 6. Comparación de Tiempos (Timeline)

```
WEBHOOKS (6-12 segundos totales):

t=0ms     Usuario dice: "Hola"
t=0-500ms   Audio streaming a Telnyx
t=500-1500ms  Espera silencio en Deepgram
t=1500-2000ms Deepgram transcripción
t=2000ms    Webhook HTTP POST enviado
t=2000-2200ms  Servidor recibe webhook
t=2200-3700ms  Claude genera respuesta
t=3700-4200ms  ElevenLabs TTS
t=4200-4500ms  S3 upload
t=4500-4700ms  Telnyx playback
t=4700-6000ms  Audio playing
═══════════════════════════════════════════════════════════════
TOTAL: 6000ms (6 segundos) ❌ TOO SLOW


WEBSOCKET (1-2 segundos totales):

t=0ms       Usuario dice: "Hola"
t=0-300ms   Audio streaming a Deepgram WSS
t=300ms     Deepgram empieza procesar (no espera silencio)
t=500ms     Transcript intermedio listo ("Hola")
t=500ms     Claude comienza procesar EN PARALELO
t=500-1000ms  Claude genera respuesta
t=500-800ms   ElevenLabs TTS EN PARALELO
t=800-900ms   S3 upload
t=900-1100ms  Telnyx playback
t=1100-1500ms Audio playing
═══════════════════════════════════════════════════════════════
TOTAL: 1500ms (1.5 segundos) ✅ REAL-TIME

MEJORA: 75% más rápido
```

---

## Leyenda

```
🔌 = WebSocket connection
🎤 = Audio input
⚡ = Processing
💬 = AI/Claude
🔊 = Audio output
📨 = HTTP webhook
📝 = Transcription
```
